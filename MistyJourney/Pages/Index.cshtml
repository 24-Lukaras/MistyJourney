@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<h2>Choose save file</h2>
<div class="savefile-carousel-container">
    <div class="savefile-card add-card" onclick="window.location = '/CreateGame'">+ New</div>
    <div class="savefile-carousel">
        <div class="savefile-card">Save 1</div>
        <div class="savefile-card">Save 2</div>
        <div class="savefile-card">Save 3</div>
        <div class="savefile-card">Save 4</div>
        <div class="savefile-card">Save 5</div>
    </div>
</div>
